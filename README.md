### JavaScript Function 

`- we can use many function in javacript . for example we can use map method .`
`- we can use many function in javacript . for example we can use map method .`
`- we can use many function in javacript . for example we can use map method .`
`- we can use many function in javacript . for example we can use map method .`
